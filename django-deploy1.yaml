apiVersion: apps/v1
kind: Deployment
metadata:
  name: django-app-2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: django-app-2
  template:
    metadata:
      labels:
        app: django-app-2
    spec:
      containers:
      - name: django
        image: it_dashboard_app
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
